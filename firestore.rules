rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the backend Admin SDK accesses attendee data (via IAM).
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
